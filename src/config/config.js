const config = {
  clientIdPaypal: process.env.CLIENT_ID_PAYPAL,
  clientIdGoogleMaps: process.env.CLIENT_ID_GOOGLE_MAPS,
  clientIdPositionStack: process.env.CLIENT_ID_POSITIONSTACK,
}

export default config
